# Simulations

In this folder you can find the simulation code.
Each experiment can consist of multiple scenarios.
Each scenario has it's own `run.sh` script to execute the simulation and copy the interesting traces to the [`/plots`](../plots) directory.
With the `run_all.sh` scripts all scenarios belonging to an experiment will be executed.
With the `run_everything` script, all experiments will be executed (might take a while).
Before executing anything ensure that enough storage (300 GB) and memory (min 8 GB) is available.

## Verification and Validation Remarks
For validation we require log data from some benchmark simulation run with help of the QUIC Interop Runner.
This needs to be executed from a seperate [repository](https://git.kom.e-technik.tu-darmstadt.de/student/theses/frisch-johannes-ma/ns-3-docker-emulation).
The pcaps generated by the ns-3 need to be copied manually for processing.
All workbench logs for this step can be generated just using the normal run scripts.

## Experiment 2 Remarks
For this experiment we also integrated BBRv3.
Since it required some refactoring that broke compatability with the current workbench setup, we implemented the required changes for BBRv3 on a seperate branch (thesis-branch-bbrv3).
There you will find just the simulation for the BBRv3 scenario, which can be executed just like all the other experiments.
After execution the logs are stored in the [`/plots`](../plots) directory and you can switch back to this main thesis branch for processing and plotting.


<!-- 
## Experiment x Remarks
Since the workbench does not natively support simulating 0-RTT handshakes, we had to implement a custom client for this experiment.
To execute this experiment switch to the seperate branch (thesis-branch-zerortt) and run the simulation.
After execution the logs are stored in the [`/plots`](../plots) directory and you can switch back to this main thesis branch for processing and plotting. 
-->